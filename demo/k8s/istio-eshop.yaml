apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: web
spec:
  selector:
    istio: aks-istio-ingressgateway-external
  servers:
    - port:
        number: 80
        name: http
        protocol: HTTP
      hosts:
        - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: web
spec:
  hosts:
    - "*"
  gateways:
    - web
  http:
    - match:
        - uri:
            prefix: /api
      route:
        - destination:
            host: publicapi
            port:
              number: 80
    - route:
        - destination:
            host: web
            port:
              number: 80
